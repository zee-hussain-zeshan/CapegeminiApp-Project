What is the module code to this module?

<%

fName = File.basename(params[:file].original_filename, ".csv")

%>

<%= form_tag import2_enrolmentcsvs_path , multipart: true do %>
	<%= text_field_tag :course_id, fName %>
	<%= hidden_field_tag :file_path, @path %>
	<%= submit_tag("Submit") %>
<% end %>